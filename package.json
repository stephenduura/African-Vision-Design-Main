{
  "name": "workspace",
  "version": "0.0.0",
  "packageManager": "pnpm@10.28.0",
  "license": "MIT",
  "scripts": {
    "preinstall": "sh -c 'rm -f package-lock.json yarn.lock; case \"$npm_config_user_agent\" in pnpm/*) ;; *) echo \"Use pnpm instead\" >&2; exit 1 ;; esac'",
    "build": "pnpm run typecheck && pnpm --filter @workspace/api-server run build && pnpm --filter @workspace/papi-foundation run build",
    "typecheck:libs": "node --max-old-space-size=4096 --max-semi-space-size=1024 ./node_modules/typescript/bin/tsc --build",
    "typecheck": "pnpm run typecheck:libs && pnpm --filter @workspace/api-server run typecheck && pnpm --filter @workspace/mockup-sandbox run typecheck && pnpm --filter @workspace/papi-foundation run typecheck && pnpm --filter @workspace/scripts run typecheck"
  },
  "private": true,
  "devDependencies": {
    "@esbuild/win32-x64": "0.27.3",
    "@rollup/rollup-win32-x64-msvc": "4.60.3",
    "@tailwindcss/oxide-win32-x64-msvc": "4.3.0",
    "lightningcss-win32-x64-msvc": "1.32.0",
    "prettier": "^3.8.3",
    "typescript": "~5.9.3"
  },
  "dependencies": {
    "stripe": "^22.2.0",
    "stripe-replit-sync": "^1.0.0"
  }
}
